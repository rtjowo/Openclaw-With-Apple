#!/usr/bin/env python3
"""生成「Tasks Import」iOS 快捷指令的安装指引。

由于 .shortcut 文件是 Apple 的二进制 plist 格式，
无法用纯 Python 方便地生成。这个脚本输出详细的手动创建步骤。

快捷指令功能:
  1. 从 iCloud Drive/Shortcuts/Tasks/ 读取 tasks_latest.json
  2. 解析 JSON 中的每个任务
  3. target=reminder → 创建提醒事项（含日期、时间、优先级）
  4. target=note → 创建备忘录
  5. 导入完成后只删 tasks_latest.json（日期归档文件 tasks_YYYY-MM-DD.json 保留）

文件机制:
  OpenClaw 每天上传两个文件:
  - tasks_2026-03-11.json  ← 归档，永久保留
  - tasks_latest.json      ← 快捷指令读这个，导入后删除
"""

def main():
    guide = """
╔══════════════════════════════════════════════════════════════╗
║          📱 Tasks Import 快捷指令 — 创建指南               ║
╚══════════════════════════════════════════════════════════════╝

📁 iCloud Drive/Shortcuts/Tasks/ 目录中的文件:
   tasks_latest.json       ← 快捷指令读取这个（导入后自动删除）
   tasks_2026-03-10.json   ← 归档，永久保留不动
   tasks_2026-03-11.json   ← 归档，永久保留不动
   ...

在 iPhone 上打开「快捷指令」App，点击右上角 + 创建新指令，
按以下步骤依次添加操作：

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

步骤 1: 获取文件
━━━━━━━━━━━━━━━
  操作: 「获取文件」(Get File)
  文件路径: /Shortcuts/Tasks/tasks_latest.json
  ☑️ 打开「仅 iCloud Drive」
  ❌ 关闭「显示文件选择器」

  💡 只读 tasks_latest.json，不碰 tasks_YYYY-MM-DD.json 归档文件

步骤 2: 获取文件内容
━━━━━━━━━━━━━━━━━━
  操作: 「获取文件中的文本」(Get Text from Input)
  输入: 步骤 1 的输出

步骤 3: 解析 JSON
━━━━━━━━━━━━━━━━
  操作: 「从输入中获取词典」(Get Dictionary from Input)
  输入: 步骤 2 的输出

步骤 4: 获取任务列表
━━━━━━━━━━━━━━━━━━━
  操作: 「获取词典值」(Get Dictionary Value)
  键: tasks
  输入: 步骤 3 的输出

步骤 5: 遍历每个任务
━━━━━━━━━━━━━━━━━━━
  操作: 「针对每一项重复」(Repeat with Each)
  输入: 步骤 4 的输出

  ┌─── 循环内部 ───────────────────────────────────────┐
  │                                                     │
  │  5a: 获取任务字段                                    │
  │  操作: 「获取词典值」键: title → 变量「标题」          │
  │  操作: 「获取词典值」键: date → 变量「日期」           │
  │  操作: 「获取词典值」键: time → 变量「时间」           │
  │  操作: 「获取词典值」键: notes → 变量「备注」          │
  │  操作: 「获取词典值」键: target → 变量「目标」         │
  │  操作: 「获取词典值」键: priority → 变量「优先级」     │
  │  操作: 「获取词典值」键: list → 变量「列表名」         │
  │                                                     │
  │  5b: 判断目标类型                                    │
  │  操作: 「如果」(If) → 变量「目标」等于 "reminder"     │
  │                                                     │
  │    ┌── 是 (reminder) ──────────────────────────┐    │
  │    │                                            │    │
  │    │  操作: 「添加新提醒事项」                     │    │
  │    │    标题: 变量「标题」                         │    │
  │    │    备注: 变量「备注」                         │    │
  │    │    列表: 变量「列表名」(如果为空则默认列表)    │    │
  │    │                                            │    │
  │    │  如果「日期」不为空:                          │    │
  │    │    操作: 「格式化日期」                       │    │
  │    │    将「日期」和「时间」拼接后格式化             │    │
  │    │    设为提醒事项的截止日期                     │    │
  │    │                                            │    │
  │    │  如果「优先级」= "high":                     │    │
  │    │    设置优先级为「高」                         │    │
  │    │                                            │    │
  │    └────────────────────────────────────────────┘    │
  │                                                     │
  │    ┌── 否则 (note) ────────────────────────────┐    │
  │    │                                            │    │
  │    │  操作: 「创建备忘录」                         │    │
  │    │    标题: 变量「标题」                         │    │
  │    │    正文: 变量「备注」                         │    │
  │    │    文件夹: 变量「列表名」(如果为空则默认)      │    │
  │    │                                            │    │
  │    └────────────────────────────────────────────┘    │
  │                                                     │
  │  结束如果                                            │
  │                                                     │
  └─── 结束循环 ───────────────────────────────────────┘

步骤 6: 删除 tasks_latest.json（仅删这个，归档文件不动）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  操作: 「删除文件」(Delete Files)
  文件: 步骤 1 的输出（tasks_latest.json）
  ❌ 关闭「确认删除」

  ⚠️ 这只会删除 tasks_latest.json
     tasks_2026-03-11.json 等归档文件会永久保留在 iCloud Drive 中

步骤 7: 显示通知
━━━━━━━━━━━━━━━
  操作: 「显示通知」(Show Notification)
  标题: ✅ OpenClaw 待办已导入
  正文: 任务已添加到提醒事项和备忘录

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📛 命名指令为「Tasks Import」

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

设置自动化（可选）:
━━━━━━━━━━━━━━━━━━
  快捷指令 App → 底部「自动化」→ + → 「特定时间」
  → 每天 22:05 → 运行「Tasks Import」→ 关闭「运行前询问」

  ⚠️ 设置为 22:05（比 OpenClaw 上传时间 22:00 晚 5 分钟）
  确保文件已上传完成再读取。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚡ 简化版（如果上面太复杂）:
━━━━━━━━━━━━━━━━━━━━━━━━━━
  如果你觉得手动创建太麻烦，也可以创建一个简化版:

  1. 「获取文件」→ /Shortcuts/Tasks/tasks_latest.json
  2. 「获取文件中的文本」
  3. 「从输入中获取词典」
  4. 「获取词典值」键: tasks
  5. 「针对每一项重复」
     → 「获取词典值」键: title
     → 「添加新提醒事项」标题设为上一步结果
  6. 「删除文件」（只删 tasks_latest.json，归档自动保留）
  7. 「显示通知」

  这个简化版把所有任务都加到提醒事项（不区分备忘录），
  不设日期和优先级，但创建过程只需 2 分钟。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📦 归档机制说明:
━━━━━━━━━━━━━━━
  每天 22:00 OpenClaw 上传两个文件:

  tasks_latest.json        ← 快捷指令读取并删除（防重复导入）
  tasks_2026-03-11.json    ← 永久归档，方便你以后回看历史待办

  iCloud Drive/Shortcuts/Tasks/ 目录随时间积累:
    tasks_2026-03-10.json
    tasks_2026-03-11.json
    tasks_2026-03-12.json
    ...（每天一个，永不覆盖）

"""
    print(guide)


if __name__ == "__main__":
    main()
